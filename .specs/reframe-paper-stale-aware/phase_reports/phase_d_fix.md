# Phase D.10 Fix Report — Claim–Evidence Corrections

- Phase: D.10 fix agent ("Staleness, Not Noise: Why Carbon-Aware LLM Pretraining Does Not Need ML Forecasting", ACM e-Energy 2027)
- Agent: FIX AGENT (write access; no experiment re-run beyond the required budget determinism re-run)
- Date: 2026-08-19
- Source of worklist: `review_d10.md` §5 (F1–F14). No BLOCKING issues existed; every finding was a wording/caption/evidence-table issue. No experiment numbers, story, or section structure changed.

---

## 1. Per-finding resolution table

| # | Severity | Finding | Resolved | Where changed |
|---|---|---|---|---|
| F1 | MAJOR | Fig. 3 caption + §4 text said "annotated lag-1 ≈0.9997 / 24 h ≈0.17"; committed figure annotates **0.9995 / 0.72** | ✅ | `publication/eenergy/main.tex` §4 (line ~347: "≈0.9995 (DE) … (≈0.72 at 24 h)") and `fig:trace` caption (lines ~371–372: "annotated at lag-1 ≈0.9995 and 24 h ≈0.72"); `claims_evidence.md` row D4; also fixed the same stale annotation in the Phase-C prose `phase_reports/phase_c.md` C.3 row |
| F2 | MAJOR | "2022–2023 contribute zero recovery at every c" false (SE-2023 c=0.75 → 0.0171) | ✅ | `main.tex` §7 c-selection paragraph ("2022 contributes zero recovery in every region, while 2023 contributes zero for DE/IT and ≤0.017 for SE"); `claims_evidence.md` G3 (cites `perYear["2023"]` SE `["0.75"]` = 0.0171) |
| F3 | MINOR | "7/8 points within one grid step" should be 8/8 in-sample | ✅ | `main.tex` §6.3 ("8/8 in-sample points lie within one grid step (IT-2023, the excluded right-censored outlier, does not)"); `grace_horizon.md` (2 spots); `phase_b5.md` (2 spots); `claims_evidence.md` F11 |
| F4 | MINOR | "135–2700 s for the sweep here" (135 nowhere in artifacts) | ✅ | `main.tex` §2 Related Work → "(148.8--2700\,s for the sweep here)"; sweep is {148.8,900,2700} (+ degenerate 150) |
| F5 | MINOR | near-unit-root claim scoped to "five grids and 2022–2026" but calibrated only DE/IT/SE | ✅ | `main.tex` abstract + contribution 1 → "in the three studied grids (DE, IT, SE)"; intro "We ground this claim…" sentence scoped to "In the three grids we study (DE, IT, SE)" for consistency; `claims_evidence.md` A1 + B3 notes |
| F6 | MINOR | "IT's material-widening regime c∈{0.25,…,2} budget-infeasible at h≥3" over-generalizes | ✅ | `main.tex` §7(c): "the train-selected $c^*{=}2$ widening is budget-infeasible at $h\ge3$, and most of the $c$-grid from $h{=}6$" (feasibleC: h=3 [0..1.5], h=6 [0..0.75]); `claims_evidence.md` G8 |
| F7 | MINOR | "12 steps … comparable to a 4σ* noise" false for SE (5.2% vs 40.6%) | ✅ | `main.tex` §6.1 → "comparable to a $4\sigma^*$ noise in the regions with material margins (DE, IT)" |
| F8 | NIT | Abstract "exceeds 0.996" (SE 0.99595 < 0.996) | ✅ | `main.tex` abstract → "lag-1 autocorrelation $\ge 0.996$" (consistent with intro); `claims_evidence.md` A1 (3-dp rounding note) |
| F9 | NIT | "36–60%" vs exact 35.8–60.5% | ✅ | `main.tex` abstract, intro, conclusion → "35.8--60.5\%" (contribution 2 already exact) |
| F10 | NIT | `claims_evidence.md` G12 references non-existent `completed_dtpr` | ✅ | `claims_evidence.md` G12 → cites `rows[].overhead_dtpr` = 200.0016 > 200 at all h (verified: IT overhead_dtpr = 200.0016 at every h) |
| F11 | NIT | SE fixed control asymmetry invisible (Known-error #2 boundary) | ✅ | `main.tex` §6.1 added disclosure sentence ("SE's fixed (published) policy uses the previously-published rounded thresholds (19, 18), $s_0{=}22.87\%$, distinct from its 2025 reopt optimum ($18.18, 17.51 \rightarrow 23.17\%$)"); new `claims_evidence.md` row F13 |
| F12 | NIT | Intro "full state on the order of 1.3–1.6 TB" (1.6 unsourced) | ✅ | `main.tex` intro → "a full state of $\approx$1.34\,TB in BF16 (671e9 parameters $\times$ 2 bytes)"; `claims_evidence.md` C3 annotated |
| F13 | NIT | "150 s control … omitted" but artifact contains the runs | ✅ | `main.tex` §3 → "computed and discarded, $\Delta S<0.01$\,pp"; `claims_evidence.md` C4 reworded |
| F14 | NIT | `budget_summary.json` IT collapse note contradicts `infeasible_budgets=[30,50]` | ✅ | `_aggregate_multiyear_budget.mjs` note generator fixed and `budget_summary.json` regenerated → note now "no within-budget AND completed point at budget(s) 30, 50% (frontier collapses at budget <= 30% on the lowest feasible edge; infeasible at B in {30, 50})"; numeric rows byte-identical; paper already correct ("B ≤ 50%"); `claims_evidence.md` E9 verified |

## 2. Artifact regeneration (F14)

- Generator: `publication/output/forecast/_aggregate_multiyear_budget.mjs` (collapse-note string, one line).
- Re-run (full pipeline + determinism check, from repo root):

```
DETERMINISM_CHECK=1 bash publication/output/forecast/run_multiyear_budget.sh
```

- Result: **Determinism check PASSED** — `multiyear_{DE,IT,SE}.{json,csv}`, `multiyear_summary.json`, `multiyear_fixed_summary.json`, `budget_summary.csv`, and `budget_summary.json` all regenerated byte-identically to the (fixed) working tree.
- vs the **original committed** artifact (`/tmp/opencode/art_snap/budget_summary.json`): the only diff is the two identical collapse `note` strings (ckpt_148_8 and ckpt_900). All numeric rows byte-identical (`first_collapse_budget=30`, `infeasible_budgets=[30,50]`, savings cells, etc.).
- `git diff` check confirmed: no other aggregate file changed.

## 3. Rebuild & verification (DoD evidence)

Commands (from `publication/eenergy`):

```
make fullclean && make
grep -c "^!" main.log        # 0
grep -c undefined main.log   # 0
pdfinfo main.pdf | grep Pages # 10
```

- LaTeX errors: **0** (`^!` count 0).
- Undefined references/citations: **0**.
- Pages: **10** (unchanged).
- Bibliography: bibtex clean (0 warnings/errors in `main.blg`; no Citation warnings in `main.log`).
- Mandatory citations: all **19** positioning-table keys still `\cite`d (verified individually; grep counts 1–6 each — unchanged by this fix, which touched no references).
- Over-claim re-grep across `main.tex`: none of "first to", "Hanford", "recovers most of the loss", "Gap never exceeds" present.
- Prose sweep: no residual "7/8", "135–2700", "1.3–1.6", or figure-annotation "0.9997/0.17" in `main.tex`, `claims_evidence.md`, `grace_horizon.md`, `phase_b0/b1_fix/b3b4/b5/c.md` (only the legitimate Table 1 φ/lag-1 values 0.9997 remain).
- Tests: `pnpm test` → **14 files, 205 tests passed** (unchanged).

## 4. DoD checklist

| DoD | Requirement | Result |
|---|---|---|
| D1 | F1–F14 all resolved (each → a diff) | **PASS** — see §1; every finding maps to an edit in `main.tex`, `claims_evidence.md`, `grace_horizon.md`, `phase_b5.md`, `phase_c.md`, or `_aggregate_multiyear_budget.mjs`/`budget_summary.json` |
| D2 | `make` rebuilds 0 errors / 0 undefined; 10 pages; mandatory citations intact | **PASS** — 0 / 0 / 10 pages / 19 keys cited |
| D3 | No experiment number changed; determinism preserved (budget_summary numeric rows byte-identical; only note text corrected) | **PASS** — see §2 |
| D4 | `claims_evidence.md` consistent with fixed paper (every row → real artifact field, matching value) | **PASS** — rows D4, G3, F11, G8, G12, A1, B3, C3, C4 corrected; new F13 row added; E9 verified against `infeasible_budgets=[30,50]` |
| D5 | `pnpm test` green (205) | **PASS** — 205/205 |
| D6 | `phase_d_fix.md` written with per-finding evidence + commands | **PASS** — this file |

## 5. Residual risks

1. **A1/abstract precision**: SE lag-1 = 0.995950, so "≥ 0.996" relies on 3-dp rounding (0.9960). This matches the SPEC reference fact and the intro; if a reviewer insists on full precision, the honest wording is "≥ 0.9960 (SE 0.99595 ≈ 0.9960 at 3 dp)" — current wording is already scoped to the three studied grids.
2. **F11 disclosure** is now explicit; no further action. The §6 degradation table itself is unchanged (SE fixed control s0 = 22.87%).
3. **F14 note text** uses ASCII "B in {30, 50}" and "<=" in the JSON (consistent with the file's existing ASCII convention); the paper's "B ≤ 50%" is untouched and correct.
4. The `phase_d.md` §DoD row claiming "grep counts 2–7 each" for the 19 mandatory citations is slightly generous (two table-only keys are cited once); presence is what the DoD requires and is confirmed.

## 6. Re-verification commands

```
cd publication/eenergy && make fullclean && make && grep -c "^!" main.log && grep -c undefined main.log && pdfinfo main.pdf | grep Pages
cd /home/simon/dev/TheGreenEpoch && pnpm test
# F14 determinism (full pipeline, ~3 min):
DETERMINISM_CHECK=1 bash publication/output/forecast/run_multiyear_budget.sh
```
